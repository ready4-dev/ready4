# Make function defaults list

make_fn_defaults_ls() is a Make function that creates a new R object.
Specifically, this function implements an algorithm to make function
defaults list. The function returns Function defaults (a list).

## Usage

``` r
make_fn_defaults_ls(fn)
```

## Arguments

- fn:

  Function (a function)

## Value

Function defaults (a list)
